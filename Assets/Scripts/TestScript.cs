using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UIElements;
public class TestScript : MonoBehaviour
{
 

    // Start is called before the first frame update
    void Start()
    {
        
    }
    // Update is called once per frame
    void Update()
    {
        

    }
    public void Open()
    {
      
    }
        

}



















































/*  NumberOfTimesClicked++;
      if (NumberOfTimesClicked == 1)
      {
          firstTime = _num;
          //disabled
          _firstButton[firstTime].GetComponent<Button>().interactable = false;
      }
if (NumberOfTimesClicked == 2)
{
  _secondButtons[_num].GetComponent<Button>().interactable = false;
  if (firstTime == _num)
  {
      score++;
      scoreText.text = score.ToString("0");
  }
  else
  {

      score--;
      scoreText.text = score.ToString("0");
      _firstButton[firstTime].GetComponent<Button>().interactable = true;
      _secondButtons[firstTime].GetComponent<Button>().interactable = true;
  }
  NumberOfTimesClicked = 0;
}

}*/